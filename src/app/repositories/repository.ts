export class Repository {
    async getAll<T>(): Promise<T> {
        return new Promise(() => { });
    }

    async getById<T>(id: number): Promise<T> {
        return new Promise(() => { });
    }

    async save<T>(value: any): Promise<T> {
        return new Promise(() => { });
    }

    async update<T>(value: any): Promise<T> {
        return new Promise(() => { });
    }
}
